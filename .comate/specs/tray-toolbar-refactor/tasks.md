# Hijessy 迭代三：托盘常驻、快捷键设置与截图工具栏重构

- [x] Task 1: 调整启动行为与托盘菜单
    - 1.1: 将 `HijessyApp::pending_start` 初始值改为 `false`，启动后保持根 viewport 隐藏
    - 1.2: 扩展 `TrayAction`，增加 `Settings` 操作
    - 1.3: 将托盘右键菜单调整为「截图」「设置」「录屏」「OCR 识别」「退出」
    - 1.4: 将录屏和 OCR 菜单项设为禁用占位
    - 1.5: 保持 Windows/macOS 托盘左键点击触发截图
    - 1.6: 验证启动后不会自动进入截图

- [x] Task 2: 实现设置面板与快捷键编辑
    - 2.1: 新增 `src/settings.rs`，封装设置面板状态和 UI
    - 2.2: 在设置面板中提供框选、全屏、窗口三项快捷键字符串编辑框
    - 2.3: 在设置面板中提供 PNG/JPG 格式选择和自动写入剪贴板开关
    - 2.4: 在设置面板中显示并允许编辑默认保存目录
    - 2.5: 点击保存时调用 `Config::save()` 并重新创建 `HotkeyManager`
    - 2.6: 从托盘点击「设置」时显示普通窗口，关闭设置后重新隐藏根 viewport
    - 2.7: 处理非法快捷键：显示错误提示并保留可继续编辑的设置面板

- [x] Task 3: 扩展标注数据模型与图像合成
    - 3.1: 在 `Annotation` 中增加 `Line { from, to, style }`
    - 3.2: 在 `Annotation` 中增加 `Pencil { points, style }`
    - 3.3: 在 compose 模块中实现直线标注烧录
    - 3.4: 在 compose 模块中实现自由折线逐段烧录
    - 3.5: 为 Line/Pencil 合成添加聚焦单元测试

- [x] Task 4: 实现直线与铅笔交互
    - 4.1: 在 `Tool` 枚举中增加 `Line` 和 `Pencil`
    - 4.2: 实现直线拖拽预览与松开提交，忽略零长度线段
    - 4.3: 实现铅笔按下后连续采样鼠标点、实时预览与松开提交
    - 4.4: 将新增标注接入现有撤销/重做栈
    - 4.5: 保证选区裁剪规则对 Line/Pencil 生效

- [x] Task 5: 重绘并重排截图工具栏图标
    - 5.1: 在 `Icon` 中增加 Line、Pencil、Ocr、Record、LongCapture
    - 5.2: 绘制参考图风格的直线、铅笔、OCR 和长截图矢量图标
    - 5.3: 绘制红色外圆加红色实心内圆的录屏图标
    - 5.4: 按「矩形、椭圆、直线、箭头、铅笔、马赛克、文本、编号」排列工具组
    - 5.5: 按「撤销、取消、保存、确认」排列操作组
    - 5.6: 将 OCR、录屏、长截图作为灰显占位按钮追加到工具栏
    - 5.7: 为所有图标保留 hover 提示，禁用项提示「即将支持」
    - 5.8: 调整胶囊工具栏尺寸与屏幕边界约束，避免窄屏溢出

- [x] Task 6: 修正默认窗口候选优先级
    - 6.1: 确认 xcap 当前平台窗口枚举的 Z 序语义
    - 6.2: 调整 `window_at`，优先返回鼠标所在的最前台有效窗口
    - 6.3: 保持无锁定状态随鼠标移动动态切换窗口
    - 6.4: 添加窗口重叠情况下的命中优先级单元测试

- [x] Task 7: 更新说明并完成质量验证
    - 7.1: 更新 README 的启动行为、托盘菜单和工具栏能力说明
    - 7.2: 执行 `cargo fmt --all --check`
    - 7.3: 执行 `cargo clippy --all-targets -- -D warnings`
    - 7.4: 执行 `cargo build --release`
    - 7.5: 执行 `cargo test --release`
    - 7.6: 检查 Git diff，确认没有无关文件或生成物

- [x] Task 8: 生成迭代三总结
    - 8.1: 将完成内容、验证结果和暂未实现能力写入 `summary.md`
